const config = {
  blog: {
    fetchingLimit: 6,
    titleLimit: 60,
    descriptionLimit: 80,
  },
  images: {
    blogCoverPlaceholderUrl: "/images/blog-cover-error-placeholder.png",
  },
};

export default config;
