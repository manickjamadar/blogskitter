const config = {
  blog: {
    initialFetchingLimit: 19,
    fetchingLimit: 12,
    titleLimit: 60,
    descriptionLimit: 80,
  },
  images: {
    blogCoverPlaceholderUrl: "/images/blog-cover-error-placeholder.png",
  },
};

export default config;
